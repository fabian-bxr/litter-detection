"""Wire contracts and shared data models.

`robodog.py` holds copies of the robodog-digipro Zenoh payload schemas;
`detections.py` models the litter_detector JSON payloads; `mission.py` holds
the agent output schemas shared across the package.
"""
