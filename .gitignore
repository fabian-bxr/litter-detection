.venv/
__pycache__/
*.pyc
data/
mlruns/
mlartifacts/
# MLflow experiment DB (binary, regenerated by training; not mergeable)
mlflow.db
findings.md
.DS_Stores
.idea/
# Ultralytics base-model weights auto-downloaded to the repo root
/yolo*.pt
# generic training checkpoint (named checkpoints are tracked)
best_model.pth
# tracked-object registry (runtime artifact)
object_registry.db
# generated run artifacts (tracker registry, findings db, mission images, sim frames;
# also YOLO training output — best weights are copied to models/)
runs/
# personal Claude Code settings (shared plans/memory in .claude/ are tracked)
.claude/settings.local.json
# Local secrets / env overrides
.env
