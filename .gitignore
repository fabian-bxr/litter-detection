.venv/
__pycache__/
*.pyc
data/
mlruns/
mlartifacts/
# generated run artifacts (tracker registry, findings db, mission images, sim frames)
runs/
.DS_Stores
# generic training checkpoint (named checkpoints are tracked)
best_model.pth
# JetBrains IDE config
.idea/
# personal Claude Code settings (shared plans/memory in .claude/ are tracked)
.claude/settings.local.json

# Local secrets / env overrides
.env
